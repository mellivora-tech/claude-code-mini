# Skill · 领域知识扩展

挂载到 **第 06 层 [提示层](../../prompt/)**。

SKILL.md 上报简短索引，模型按需触发读取，内容并入上下文，**纯信息无副作用**。

## 用户视角
让它更懂 PDF / Excel / 特定任务 —— 纯文档，不写代码不起服务。
操作方式：放一份 SKILL.md。

## 与扩展层的关系
遵循 [`../`](../) 的注册 + 延迟加载原则：先注册简短索引，被需要时才读入正文。
