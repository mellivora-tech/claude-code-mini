# MCP · 外部能力扩展

挂载到 **第 02 层 [工具层](../../tools/)**。

外部 server 上报工具 schema，注册进工具列表；[编排层](../../agent/) 统一调度并转发执行，
**产生真实副作用**。

## 用户视角
接 GitHub / 数据库 / 内部系统 —— 给系统「装新工具」。
操作方式：配置 MCP server。

## 与扩展层的关系
遵循 [`../`](../) 的注册 + 延迟加载原则：先注册轻量条目，被调用时才执行。
