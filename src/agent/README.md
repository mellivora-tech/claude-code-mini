# 03 · 编排层 · AGENT LOOP

「发请求 → 工具调用 → 本地执行 → 结果回填 → 再发请求」的状态机，
直到模型不再请求工具。

## 职责
- 驱动模型与工具之间的循环
- 调度工具执行、把结果回填进对话历史
- 判断循环终止条件（模型不再请求工具）

## 与其他层的关系
- 调用 [`model`](../model/) 获取下一步决策
- 调度 [`tools`](../tools/) 执行工具调用
- 循环中把历史交给 [`context`](../context/) 做压缩 / 截断
