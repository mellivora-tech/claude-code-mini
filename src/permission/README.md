# 05 · 权限层 · PERMISSION GATE

横切于工具层之上的拦截机制。危险操作（删除、push、网络请求）需用户确认，
不影响模型思考，只影响能否落地。

## 职责
- 在工具执行前拦截，判断是否需要用户确认
- 维护允许 / 禁止列表（白名单 / 黑名单）

## 与其他层的关系
- 拦截 [`tools`](../tools/) 的调用
- 确认交互通过 [`interface`](../interface/) 呈现给用户
